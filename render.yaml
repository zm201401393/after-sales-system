services:
  - type: web
    name: after-sales-system
    runtime: node
    rootDir: server
    buildCommand: cd ../client && npm install && npm run build
    startCommand: node index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
