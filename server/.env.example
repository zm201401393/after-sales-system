# 复制本文件为 .env 并填入真实值（.env 不会被提交到 git）
# AI 协商功能用的大模型网关（OpenAI 兼容格式）。没配置会自动降级到规则引擎。

# 大模型 API Key（京东云网关，pk- 开头）
AI_API_KEY=pk-xxxxxxxxxxxxxxxxxxxxxxxx

# 接口地址（OpenAI 兼容，/v1 结尾，不要带 /chat/completions）
AI_BASE_URL=http://ai-api.jdcloud.com/v1

# 模型名
AI_MODEL=GLM-5.1

# 可选：后端端口，默认 3000
# PORT=3000
